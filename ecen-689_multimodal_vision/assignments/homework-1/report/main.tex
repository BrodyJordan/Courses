\documentclass{article}

\usepackage[letterpaper, margin=0.75in]{geometry}
\usepackage{hyperref}
\usepackage{array, booktabs, siunitx}
\usepackage{fancyhdr}
\usepackage{listings}
\usepackage{xcolor}

\date{}
\headsep=0.25in
\pagestyle{fancy}
\fancyhf{}

\fancyhead[L]{ECEN-689}
\fancyhead[C]{Homework 1: 3DGS}
\fancyhead[R]{Brody Jordan}

\lstset{language=Python}

\begin{document}
\section{Environment}
I trained this model on a Windows 11 machine with an NVIDIA RTX 5080 GPU using Conda with Python 3.9.25 and CUDA 12.8. This open issue has been particularly helpful in resolving Python/CUDA dependency issues: \url{https://github.com/graphdeco-inria/gaussian-splatting/issues/1215}.
\section{Data Split}
I utilized the \verb|"--eval"| flag to automatically split the dataset for evaluation.
\section{Metrics}
I ran several iterations and the best of which achieved the following metrics.
\begin{figure}[hbpt!]
    \centering
    \begin{tabular}{ @{} lll @{} }
        \toprule
        Metric    & Value     & Reference Target \\
        \midrule
        PSNR (dB) & 22.797117 & $\geq$ 20.0      \\
        SSIM      & 0.8152919 & $\geq$ 0.80      \\
        LPIPS     & 0.1899525 & $\leq$ ?         \\

        \bottomrule
    \end{tabular}
    \vspace{-0.15in}
\end{figure}

\section{Screenshots}

\section{Analysis}

Training took, on average, around 6 minutes and 30 seconds to complete at 30,000 iterations. Using the \verb|"--optimizer_type sparse_adam"| flag brought the average training time down to around 3 minutes.

% \begin{lstlisting}
% python convert.py -s path_to_data
% python train.py -s path_to_data --eval -m output_model_path
% python render.py -m input_model_path --eval
% python metrics.py -m input_model_path
% \end{lstlisting}

\end{document}